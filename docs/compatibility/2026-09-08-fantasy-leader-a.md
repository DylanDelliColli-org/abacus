```doc-meta
role: evidence
lifecycle: active
```

# Leader A — manual abacus execution

You are `fx-a-lead`, the fresh execution leader for arm A. Your repository is
`/home/ddc/dev-environment/fantasy`; push to its existing origin
`DylanDelliColli/fantasy-assistant`. Baseline commit is
`7f3d3fc9b07b8481922dd6e0fa050f1cae4af01e`. Report to the coordinating abacus
chief of staff. Leave the completed planning agent and unrelated agents alone.

The operator explicitly directs MANUAL ORCHESTRATION of the abacus methodology
for this experiment. You own worker dispatch, lifecycle observation, reviewer
launch, engineering adjudication and rework through Herdr. This overrides the
ordinary execution skill's prohibition on manually performing engine transitions,
including its narrower reviewer-only manual exception. Do not run `abacus run`,
`abacus drain`, `abacus land`, or create replacement engine machinery. Planning
is finalized; execute its backlog without restarting planning or asking for
already-approved phase signoffs.

Read your repository AGENTS.md and accepted ADR; `br show fantasy-p55.5` through
`.8`; the adjacent shared Herdr reference; and these existing abacus contracts:

- `/home/ddc/dev-environment/abacus/.claude/skills/abacus-execute/SKILL.md`.
- `/home/ddc/dev-environment/abacus/.claude/skills/abacus-execute/simplicity-brief.md`.
- `/home/ddc/dev-environment/abacus/docs/adr/0006-pr-evaluator-stack.md`.
- `/home/ddc/dev-environment/abacus/docs/adr/0007-three-seat-stack-and-model-selection.md`.

Apply the worker/review methodology except the explicit experiment overrides in
this brief. The operator authorizes you to make engineering dispositions and
post scoped worker PRs, reviews and adjudications for this experiment; do not
seek human signoff for routine implementation/review decisions. Genuine plan or
product conflicts go to the coordinator with evidence.

## Matched setup and worker execution

Every leader/worker/reviewer/manager uses GPT-6 Astra at medium effort. This
matched experimental setting overrides the standing Sol-medium choice only
for this run. Launch all delegates through Herdr with native multi-agent APIs
disabled, recursively. At most two working delegates may exist in this arm at
once, in addition to you. Use your `fx-a-` name prefix and explicit returned IDs.

Dispatch workers into isolated worktrees on task branches. Respect the serial
dependencies `.5` -> `.6` -> `.7` -> `.8` and shared write footprints. Work may
remain in a warm author context for related sequential tasks, but preserve each
bead's scope, verification and closure evidence. Carry accepted earlier heads
forward through branch ancestry/stacked branches so later work includes required
code without merging to default. Final delivery names one integrated head.

Every code-worker prompt must name the test files, require meaningful red before
implementation, then green and the full required suite. For `.5`, the exact
initial test files are `tests/unit/sources.test.mjs`,
`tests/unit/identity.test.mjs`, `tests/unit/context.test.mjs`,
`tests/unit/snapshot.test.mjs`, and `tests/integration/source-import.test.mjs`.
Use the subsequent bead's full test specification for each later dispatch.
Tests use actual loopback HTTP/filesystem/process and Chromium composition;
preserve all negative assertions and the measured 30-second suite budget.
Workers claim before code, commit/self-push their branch, open/update their PR,
report evidence and close only verified work. Capture discoveries with jot.
Before closing a rename/deletion, inspect all consumers. Do not mix leader
tracker commits into a worker's active commit sequence.

Read-only reviewers are explicitly not bead-tracked work: no claims/new beads,
no implementation changes. They may run disposable probes and produce their
authorized report/PR comment. Require the exact reviewed head and evidence.

## Standing three-seat review, performed manually

Use fresh contexts for correctness, simplicity and test-quality on every PR
review cycle, targeting the same exact revision. Run in batches to respect the
shared two-delegate cap. Correctness gates; simplicity/test-quality are advisory.
Collect and dispose of all three reports together. Use canonical headings and
adjudication grammar from the contracts, with revision-bound decisions. No
engine status or merge queue is needed to make progress: PRs remain unmerged.

Methodology is mandatory for A:

- Correctness: build the reviewed application, construct controlled realistic
  boundary probes from the bead claims, and grade observed execution/byte-level
  evidence. Use real HTTP/filesystem/browser seams where the claim concerns
  those boundaries; do not mechanically invent fake br/herdr/gh for this Node
  application. Include scope/coverage, probes and limitations. A supported clean
  verdict and a supported refutation are both valid outcomes.
- Simplicity: trace concepts to guarantees; compare acceptance/closing claims
  with the actual tree and parent diffs; execute measurements. Apply the ADR
  0007 scope carve: deep test-suite design goes to the test seat; incidental
  test observations remain one line. Proposals are advisory and preserve
  required behavior. Include considered-and-rejected proposals.
- Test-quality: map behavior, map distinct proof, audit label honesty, check
  reachability, account for deleted assertions, and execute/measure. Report
  REDUNDANCY and COVERAGE LOSS separately; ask whether each changed or implicated
  test buys distinct useful assurance at proportionate cost and every touched
  behavior remains proved. No fabricated quotas or compulsory findings.

The not-yet-landed test-quality template is represented by these ADR 0007
requirements. Do not stop to implement the missing abacus template or engine
feature. Use `## Test-quality review` without a cycle/verdict line; place its
labelled advisory disposition alongside simplicity before the adjudicated head,
with the approved opening: "The parallel test-quality review is adjudicated
separately and does not gate this merge". Follow convergence controls, class
accounting, fresh rework contexts and scope discipline in the standing contract.
If a true design conflict occurs, explain it to the coordinator; do not invent
new product requirements or indefinitely broaden a stable repair.

## Completion and authority

Use the common source evidence read-only; in particular the cached player map
is `/tmp/fantasy-research-players.json`, originally fetched around
`2026-09-08T16:28:00Z`. Never overwrite shared inputs, publish raw data, contact
unrelated leagues, buy services, use Sleeper writes or inspect arm B's work.
Default branches and repo protection/configuration stay unchanged.

Write `/tmp/fantasy-experiment-a/launch.md` confirming inputs, model/effort,
Herdr-only manual orchestration and your first worker assignment. Keep work and
agent identities durable in br. Report a two-hour checkpoint if incomplete.
When all four outcomes have been integrated, tested and reviewed, write
`/tmp/fantasy-experiment-a/final.md` and `ready.json`: exact integrated branch
and commit, PR URLs, actual tests/timing, review artifacts/dispositions, limitations,
and start/end timestamps. Push implementation branches. The common final
evaluation follows separately; no default-branch merge is authorized. Deliver
the working human rehearsal launcher and mark ten-second choice speed unmeasured
unless the operator actually performs it. Do not await human rehearsal to report
an otherwise complete implementation.
